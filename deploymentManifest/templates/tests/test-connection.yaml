apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deploymentManifest.fullname" . }}-test-connection"
  labels:
    {{- include "deploymentManifest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deploymentManifest.fullname" . }}:{{ .Values.port }}']
  restartPolicy: Never
