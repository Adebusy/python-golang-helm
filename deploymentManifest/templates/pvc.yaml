
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.image.repository }}-pvc
  namespace : "{{ .Values.appNamespace }}-ns"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: manual